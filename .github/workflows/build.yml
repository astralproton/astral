name: Build Status Report

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  report-build-status:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Confirm workflow is running
        run: echo "✅ Workflow ejecutado correctamente en Ubuntu 24.04"

